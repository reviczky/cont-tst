\starttext

\startluacode

    local result, w, h, d, bbox = metapost.simple("metafun",[[ draw fullcircle scaled 1cm ;]])

    context("w = %s",w)
    context.par()
    context("h = %s",h)
    context.par()
    context("d = %s",d)
    context.par()
    context("b = [ %, t ]",bbox)
    context.blank()
    context("%s",result)
    context.par()

\stopluacode

\stoptext
